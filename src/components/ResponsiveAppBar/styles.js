import {makeStyles} from '@mui/styles'

export default makeStyles(() => ({
  
  root: {
    flexGrow: 1,
    backgroundColor: "inherit",
    color: "black"
  },
  main: {
    color: "#ccc"
  }

 
}));